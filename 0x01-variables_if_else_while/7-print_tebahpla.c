#include <stdio.h>

/**
 * main - entry point
 *
 * Description: print reverse numbers
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int c;

	for (c = 'z'; c >= 'a'; i--)
	{
		putchar(c);
	}
	putchar('\n');
	return (0);
}

#include "main.h"

/**
 * length - get length of string
 *
 * @str: the string
 *
 * Return: length
 */

int length(char *str)
{
	int x;

	for (x = 0; str[x] != '\0'; x++)
	{
		;
	}
	return (x);
}

/***/

char *argstostr(int ac, char **av)
{
	int f, g, h, d;
	char *m;

	if (ac == 0 || av == NULL)
	{
		return (NULL);
	}
	for (f = 0; f < ac; f++, g++)
	{
		g ++www
	}
}
